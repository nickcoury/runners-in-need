# Browser Audit - 2026-03-25

## Status

This file now reflects the project after the March 25 follow-up fixes.

The findings from the original browser audit were addressed:

- Local public pages no longer crash without a `.env`
- Homepage filtering now shows the no-results state correctly
- Need cards now link to organization profiles
- Need detail pages now include a copy-link action
- Anonymous pledge success now explains follow-up and offers account creation
- `/about` now includes in-content conversion CTAs
- Mobile contrast issues from the prior pass were fixed

Additional follow-up improvements also shipped:

- The homepage hero is now donor-first instead of organizer-first
- `/api/health` now returns `200` with explicit degraded status when the DB is unavailable
- Donor dashboards now say `My Pledges` instead of `Incoming Pledges`

## Verification

Commands and checks run during the follow-up:

```bash
npm run build
npx playwright test tests/e2e/smoke.spec.ts --config=playwright.config.ts
npx playwright test tests/e2e/prod-smoke.spec.ts --config=playwright.prod.config.ts
```

Results:

- Local smoke suite passed `7/7`
- Production smoke suite passed `5/5`

Targeted browser verification also confirmed:

- Local `/` and `/about` return `200` without a local `.env`
- Local `/api/health` returns JSON with `status: "degraded"` instead of failing
- Local `/api/needs` degrades to an empty list instead of throwing when DB env is missing
- Homepage search shows `No results found` when all cards are filtered out
- Need-card org names render as links
- `/about` contains direct `Browse Needs` and `Become an Organizer` CTAs
- The mobile contrast issues reproduced in the first pass no longer appear in the targeted axe check

## Open Items

No open issues remain from the March 25 audit pass.
